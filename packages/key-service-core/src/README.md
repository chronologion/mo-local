# mo-key-service-core

Rust core implementation for the Key Service (canonical CBOR formats, AAD registry, KeyVault formats and merge, ciphersuite registry, and session policy engine) per `docs/rfcs/rfc-20260107-key-service-core.md`.

This crate is consumed by the WASM wrapper (`mo-key-service-wasm`) and is not a standalone binary.
